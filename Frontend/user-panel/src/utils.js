import { config } from './service/config';
import { config } from "./service/config";

export function createUrl(path) {
  return `${config.serverUrl}/${path}`;
import { config } from './Service/config'

export function createUrl(path) {
  return `${config.serverUrl}/${path}`
}
